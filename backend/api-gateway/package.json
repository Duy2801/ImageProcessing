{
  "name": "api-gateway",
  "version": "1.0.0",
  "description": "Single public API Gateway facade for the image processing services",
  "main": "src/lambda.js",
  "private": true,
  "scripts": {
    "package": "serverless package",
    "deploy": "serverless deploy --stage prod --conceal",
    "remove": "serverless remove --stage prod"
  },
  "devDependencies": {
    "serverless": "^3.40.0"
  },
  "engines": {
    "node": ">=20"
  }
}
